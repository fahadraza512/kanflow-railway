/**
 * @deprecated — use useNotificationStream instead.
 * This file is kept only so existing imports don't break.
 * RealtimeNotificationsProvider now calls useNotificationStream directly.
 */
export function useRealtimeNotifications() {
    return null;
}
